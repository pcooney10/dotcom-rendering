type Props = {
    // TODO: Extend these suggested props
    // designType: DesignType;
    // pillar: Pillar;
    // edition: Edition;
    // isImmersive: boolean;
};

export const decideTheme = ({  }: Props): ThemeType => {
    const theme = {};

    // E.g.: if (isImmersive) theme.headline.backgroundColour = 'black';

    return theme;
};
